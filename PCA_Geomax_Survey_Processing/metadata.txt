# This file contains metadata for your plugin.

# This file should be included when you package your plugin.# Mandatory items:

[general]
name=PCA Geomax Survey Processing
qgisMinimumVersion=3.0
description=PCA Geomax Survey Processing is an internal QGIS Plugin specifically built for PCA survey data management and processing purposes.
version=2.01
author=Valerio Pinna
email=vpinna@pre-construct.com

about=PCA Geomax Survey Processing is an internal QGIS Plugin specifically built for PCA survey data management and processing purposes.

	  It is a component of the new DRS/GIS integrated data management system, and it is based both in the new Geomax codelist system and in the raw survey data exported on shapefile format. 
	  
	  The current version of the plugin is compatible with the Geomax codelist version draft 4.2. A dedicated help in the toolbar provides more inforamtions and detailed use procedure.

tracker=https://github.com/PCA-Geodata/PCA_Geomax_Survey_Processing_QGIS_Plugin/issues
repository=https://github.com/PCA-Geodata/PCA_Geomax_Survey_Processing_QGIS_Plugin
# End of mandatory metadata

# Recommended items:

hasProcessingProvider=no
# Uncomment the following line and add your changelog:
changelog=2.01
 - Added exception for new DRS_Context_Database name which replaces the previous DRS_Table
 - Added exception for new DRS_Trench_Database name which replaces the previous DRS_Trench_Sheet
 - Updated Help with new DRS databases names
 <p>2.0
 Main update release after testing:
 - New Help. Updated images, text and style
 - Plugin open Geomax zip files without the need to unzip them
 - Temporary survey group name on GIS now uses the job name
 - If a temporary group name with the same job name already exists, a suffix _1, _2, etc will be added
 - Layers Points_***_PLY and Points_***_LN will be processed but discarded from project importing
 - Interventions Lines added as polygon to the GIS layer by the tool 'Copy to GIS layers'
 - Added field 'sec_level' to section lines with value retrieved from Points_processed
 - Tool 'Copy to GIS layers' now works fine in cases where there are two processed survey layers with the same name
 - Updated PCA logo on toolbar
 - Added PCA logo and icons to dialogs.
 - Fixed type error on successfull backup message
 - Solved exceptions on geopackage backup (unselected gpkg layer) 
 - Fixed bug on DRS tables update when they were already editable
 <p>1.22
 - Added eception for Z field when generated by PT-Elev instead PT-Height
 <p>1.21
 - Help updated with new spreadsheet updater: 
 <p>1.2
 - New DRS spreadsheet updater:  
	- separation between DRS context and DRS trenches;
	- issue with multiarea and multiDRS sites solved.
 <p>1.04
  - Added messageBar during layer uploading
 <p>1.03
 - Updated dialogs and help styles
 <p>1.02
 - First released on 05/09/2022 on the PCA Repository 
			


# Tags are comma separated with spaces allowed
tags=python PCA Geomax survey 

homepage=https://github.com/PCA-Geodata/PCA_Geomax_Survey_Processing_QGIS_Plugin
category=Plugins
icon=icons/geomax_processing_copy_paste_icon.png
# experimental flag
experimental=False

# deprecated flag (applies to the whole plugin, not just a single version)
deprecated=False

# Since QGIS 3.8, a comma separated list of plugins to be installed
# (or upgraded) can be specified.
# Check the documentation for more information.
# plugin_dependencies=

Category of the plugin: Raster, Vector, Database or Web
# category= Plugin

# If the plugin can run on QGIS Server.
server=False

